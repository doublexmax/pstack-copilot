name: check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  port-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/check-copilot-port.mjs
